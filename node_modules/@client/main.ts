let template = r => `
<h1>Some title!</h1>
<p>Some sort of text!</p>
<p>Message from da server: ${r.message}</p>
`;

fetch("/test")
	.then(r => r.json())
	.then(r => {
		document.body.innerHTML = template(r);
	});