import * as express from "express";

let app = express();

app.use("/", express.static("static"));

app.get("/api", (req, res) => {
	res.write(JSON.stringify({
		message: "Hello!"
	}));
})

console.log("This is the server main!");

app.listen(8000, "localhost");